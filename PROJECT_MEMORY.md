## 2026-06-02
- Added per-printer `pausedAsActive` option to distinguish paused prints from idle behavior.
- Updated status evaluation so `paused` can be treated as active when configured.
- Extended configuration normalization, schema, and tests for the new option.
